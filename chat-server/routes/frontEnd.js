const router = require('express').Router()
// const frontEnd = require('../public/index.html');

// router.get('*', (req, res) => {
//     res.sendFile(frontEnd, { root });
// });

module.exports = router;